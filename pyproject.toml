[build-system]
requires = [
    "setuptools>=61.0",
    "setuptools_scm[toml]>=3.4.3",
    "wheel",
]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
version_scheme = "release-branch-semver"
local_scheme = "no-local-version"

[project]
name = "asimov-simplepe"
dynamic = ["version"]
authors = [
    { name = "Daniel Williams", email = "daniel.williams@glasgow.ac.uk" },
]
description = "Simple-PE pipeline integration for Asimov"
readme = "README.md"
license = { file = "LICENSE" }
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: Science/Research",
    "Topic :: Scientific/Engineering :: Astronomy",
    "Topic :: Scientific/Engineering :: Information Analysis",
    "Natural Language :: English",
]
requires-python = ">=3.10"
dependencies = [
    "asimov>=0.7",
]

[project.optional-dependencies]
asimov = [
    "asimov>=0.7",
]
docs = [
    "sphinx",
    "kentigern >= 0.3.0",
    "numpydoc",
]
test = [
    "pytest>=7.0.0",
    "pytest-cov",
]

[project.entry-points."asimov.pipelines"]
simplepe = "asimov_simplepe:SimplePE"

[project.urls]
"Source code" = "https://github.com/transientlunatic/asimov-simplepe"
"Documentation" = "https://transientlunatic.github.io/asimov-simplepe/"

[tool.setuptools]
packages = ["asimov_simplepe"]

[tool.setuptools.package-data]
asimov_simplepe = ["configs/*.ini"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = "--cov=asimov_simplepe --cov-report=html --cov-report=term"
